[project]
name = "librhub"
version = "0.1.0"
description = "Blog and content sharing platform for developers"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.128.8",
    "uvicorn>=0.40.0",
]
